{
  "name": "@sprice237/firebase-auth-demo",
  "version": "0.1.0",
  "author": "Sean Price <sean@seanprice.io>",
  "license": "MPL2",
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "build": "yarn workspaces foreach -vpt run build",
    "clean": "yarn workspaces foreach -vp run clean",
    "lint": "yarn workspaces foreach -vp run lint"
  },
  "engines": {
    "node": "14"
  },
  "devDependencies": {
    "typescript": "4.3.5"
  }
}
