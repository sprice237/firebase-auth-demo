type Organization {
  id: ID!
  name: String!
}

type Query {
  me: User!
  organizations: [Organization!]!
  organizationsForUser(userId: ID!): [Organization!]!
}

type User {
  name: String!
  email: String!
  firebaseUid: String!
}
